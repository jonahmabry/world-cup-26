---
"world-cup-26": patch
---

Show each resolved team's frozen FIFA World Ranking (`#<position>`) in the projected bracket, right-aligned in a fixed gutter so rankings form a clean column across rows. Placeholder slots (TBD / Winner-of) and unranked names render no number.
